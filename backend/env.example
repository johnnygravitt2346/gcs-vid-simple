# Trivia Factory Cloud-Native Environment Variables
# Copy this to .env and fill in your values

# Cloud Identity (REQUIRED)
GOOGLE_CLOUD_PROJECT=<YOUR_PROJECT_ID>
GCS_BUCKET=<YOUR_BUCKET>
CHANNEL_ID=default

# API Configuration
API_BASE_URL=http://localhost:8000
API_HOST=0.0.0.0
API_PORT=8000

# Streamlit UI Configuration
STREAMLIT_PORT=8501

# VM Configuration (set this to your VM's external IP)
VM_EXTERNAL_IP=<YOUR_VM_EXTERNAL_IP>

# Gemini AI (optional for first test)
GEMINI_API_KEY=<YOUR_GEMINI_API_KEY>

# Service Account (should be attached to VM)
# No need to set GOOGLE_APPLICATION_CREDENTIALS if using attached service account

# Path Configuration (auto-generated from above)
# GCS_BASE=gs://${GCS_BUCKET}
# GCS_CHANNELS=${GCS_BASE}/channels/${CHANNEL_ID}
# GCS_JOBS=${GCS_BASE}/jobs
# SCRATCH_ROOT=/var/trivia/work
